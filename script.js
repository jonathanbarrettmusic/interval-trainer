/*
 * script.js — Interval Trainer (stub)
 * Full implementation added in subsequent steps.
 *
 * Architecture (to be filled):
 *   intervals.js logic → inline module INTERVALS
 *   renderStave(note1, note2, clef, container) → VexFlow
 *   generateQuestion(settings) → question object
 *   UI wiring — event listeners, score state, filter reads
 */

document.addEventListener('DOMContentLoaded', () => {
  document.getElementById('question-prompt').textContent =
    'Interval logic loading in next step…';
});
